'use strict'

/*alert("Hello world");

let a = 10;
a+=5;

console.log(a);

let b =prompt('geef je leeftijd');;



if(b >= 18){
    console.log('Welkom')
}else{
    console.log('tu pue la merde')
}
function controleerLeeftijd(event){
    event.preventDefault();
  
    let leeftijd = document.getElementById('leeftijd').value;
    if(leeftijd < 18){
      console.log('Je bent te jong');
    }else{
      console.log('Welkom');
    }
  }
  
  const form = document.getElementById('form');
  form.addEventListener('submit', controleerLeeftijd);

  function ucFirst(word){
    console.log(word.slice(0,1).toUpperCase() + word.substr(1));
  }
  ucFirst('huh are you talking about')*

  function ucFirst(word){
    word = word.trim();
    let words = word.split(" ");
  
  //  for(let part = 0; part < words.length; part++){
  
    for(let part in words){
      words[part] = words[part].slice(0,1).toUpperCase() + words[part].substr(1);
    }
  
    console.log(words.join(" "));
  
  }
  
  let name1 = 'mike derycke';       // Resultaat in console: 'Mike Derycke'
  let name2 = '   max power    ';   // Resultaat in console: 'Max Power'
  let name3 = ' judas de verrader'; // Resultaat in console: 'Judas De Verrader'
  
  ucFirst(name1);
  ucFirst(name2);
  ucFirst(name3);

  let string = "Javascript is the main focus of Web Essentials"
  console.log(string.replace('Essential','Advanced'));


  function longestString(...list){
    let longest = '';
  
    for(let country in list){
      if(longest.length < list[country].trim().length){
        longest = list[country].trim();
      }
    }
  
    return longest;
  }
  
  console.log(longestString('            België              ', 'Nederland', 'Spanje', 'Verenigde Staten van Amerika'));

  const months =["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
  const d = new Date();

  let month = months[d.getMonth()];

  console.log(d.getDate()+' '+ month+' '+ d.getFullYear());*/
  
